# Yang's Blog 项目文件说明

这份文档说明博客项目中各个主要文件和文件夹的用途。日常维护时，通常只需要关注个人配置、Markdown 文章和 `public` 静态资源。

## 日常最常使用的文件

| 路径 | 作用 | 是否建议编辑 |
| --- | --- | --- |
| `app/site.config.ts` | 统一管理博客名称、首页欢迎语、个人资料、教育经历、兴趣、邮箱和社交链接 | 经常 |
| `content/posts/` | 存放公开文章，一篇 `.md` 文件对应一篇网页文章 | 经常 |
| `content/post-template.md` | 新文章模板，复制到 `content/posts` 后修改 | 经常 |
| `content/README.md` | Markdown 写作和发布说明 | 查阅 |
| `app/data.ts` | 管理项目展示和资源下载列表 | 按需 |
| `public/` | 存放头像、首页图片、文章图片、分享图和下载文件 | 按需 |

## Markdown 文章

### `content/posts/`

网站正式读取的文章目录。当前包含四篇示例文章：

- `unet-notes.md`：U-Net 技术笔记。
- `spring-boot-review.md`：Spring Boot 学习复盘。
- `july-campus.md`：校园生活随笔。
- `game-journey.md`：游戏体验记录。

文章文件开头的 `---` 区域保存标题、日期、分类、标签和草稿状态；后面是 Markdown 正文。网站会自动生成首页文章列表、归档、分类、标签、目录、阅读时间和前后篇导航。

### `content/post-template.md`

新文章模板。复制到 `content/posts` 后，把文件名改成简短的英文地址，例如 `my-first-post.md`，再填写文章信息和正文。

### `content/README.md`

文章系统使用说明，记录目前支持的 Markdown 格式和发布方法。

## `app`：网站页面与功能

| 文件 | 作用 |
| --- | --- |
| `app/page.tsx` | 博客首页，包括城市背景、打字标题和文章列表 |
| `app/layout.tsx` | 全站基础结构、网页标题、描述和分享信息 |
| `app/site.config.ts` | 统一个人信息配置 |
| `app/components.tsx` | 顶部导航、页脚和文章卡片等公共组件 |
| `app/globals.css` | 全站颜色、字体、布局、移动端适配和动画样式 |
| `app/TypingTitle.tsx` | 首页逐字显示标题的动画 |
| `app/MarkdownContent.tsx` | 把 Markdown 转换为网页中的标题、段落、列表、代码、表格和图片 |
| `app/lib/posts.ts` | 自动读取文章、解析文章信息、排序并生成分类和标签 |
| `app/sitemap.ts` | 自动生成供搜索引擎读取的站点地图 |
| `app/not-found.tsx` | 访问不存在页面时显示的 404 页面 |
| `app/chatgpt-auth.ts` | 托管环境需要的访问支持，通常不要修改 |

### 页面目录

| 路径 | 对应页面 |
| --- | --- |
| `app/about/page.tsx` | 关于我 |
| `app/posts/page.tsx` | 文章归档、分类和标签 |
| `app/posts/[slug]/page.tsx` | 单篇文章详情模板 |
| `app/projects/page.tsx` | 项目列表 |
| `app/projects/[slug]/page.tsx` | 单个项目详情 |
| `app/resources/page.tsx` | 学习资源和下载页面 |

`[slug]` 表示动态地址。例如 `content/posts/unet-notes.md` 会生成 `/posts/unet-notes` 页面。

## `public`：图片和下载资源

| 文件或目录 | 作用 |
| --- | --- |
| `public/city-hero-wide.jpg` | 首页城市背景图 |
| `public/og-blog.png` | 分享博客链接时显示的预览图 |
| `public/favicon.svg` | 浏览器标签页图标 |
| `public/downloads/` | 对外提供下载的学习资料 |
| `public/file.svg`、`globe.svg`、`window.svg`、`og.png` | 早期或备用资源，当前通常不需要编辑 |

文章图片建议放入 `public/images/`。Markdown 中可以这样引用：

```markdown
![图片说明](/images/example.jpg)
```

头像放入 `public` 后，在 `app/site.config.ts` 中填写路径，例如：

```ts
avatar: "/avatar.jpg",
```

## 构建、测试与发布

| 文件或目录 | 作用 |
| --- | --- |
| `package.json` | 记录网站依赖和构建、测试命令 |
| `package-lock.json` | 锁定依赖版本，保证不同环境结果一致 |
| `vite.config.ts` | 网站主要构建配置 |
| `next.config.ts` | Next.js 兼容配置 |
| `tsconfig.json` | TypeScript 类型与编译配置 |
| `postcss.config.mjs` | CSS 处理配置 |
| `eslint.config.mjs` | 代码检查规则 |
| `tests/rendered-html.test.mjs` | 自动检查首页、文章归档和 Markdown 页面 |
| `build/` | 网站构建插件和辅助代码 |
| `worker/` | 线上运行入口 |
| `.openai/hosting.json` | 当前网站托管配置，不要随意修改 |

## 数据库预留文件

| 文件或目录 | 作用 |
| --- | --- |
| `db/` | 数据库连接与结构预留 |
| `drizzle/` | 数据库迁移记录 |
| `drizzle.config.ts` | 数据库工具配置 |
| `examples/d1/` | 数据库功能示例，不参与当前博客 |

当前博客文章来自 Markdown 文件，没有使用数据库。以上文件保留给将来的评论、后台管理或在线编辑功能。

## 自动生成或内部目录

| 目录 | 作用 | 注意事项 |
| --- | --- | --- |
| `dist/` | 构建生成的线上版本 | 会自动重新生成，不要手动编辑 |
| `node_modules/` | 网站依赖程序 | 不要手动修改 |
| `.git/` | 版本历史 | 不要手动修改 |
| `.wrangler/` | 本地构建缓存 | 通常不用处理 |
| `.agents/` | 工作环境内部配置 | 通常不用处理 |
| `src/content/` | 早期内容目录 | 当前正式文章系统不读取这里 |

## 日常维护流程

1. 修改个人资料：编辑 `app/site.config.ts`。
2. 新建文章：复制 `content/post-template.md` 到 `content/posts/`。
3. 添加图片：放入 `public/images/`，在文章中使用 `/images/文件名`。
4. 修改项目和资源：编辑 `app/data.ts`。
5. 完成后重新构建并发布网站。

如果只是写文章或修改个人资料，通常不需要编辑其他程序文件。
